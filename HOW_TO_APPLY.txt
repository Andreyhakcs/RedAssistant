1) Замените файл red2/splash.py из архива (решает чёрный экран видео на старте).
2) Откройте red2/app.py и вставьте функции из app_patch.txt (исключает одновременную озвучку).
   - Добавьте метод _speak()
   - Замените озвучку в _on_vision_ready() на self._speak(...)
   - Замените озвучку в _on_llm_reply() на self._speak(...)
